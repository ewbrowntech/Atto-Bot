import os

storagePath = os.path.join(os.getcwd(), "storage")
statusMessaging = {
    "audioCommence": True,
    "audioComplete": True,
    "videoCommence": True,
    "videoComplete": True,
    "transcodeCommence": True,
    "transcodeComplete": True,
    "stitchCommence": True,
    "stitchComplete": True,
}
